[project]
name = "chatbot"
version = "0.1.0"
description = "Akwaaba Guide — interactive terminal chatbot for Ghana culture, tourism & language."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.0.0",
]
